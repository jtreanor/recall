# Recall — Gemini CLI Instructions

- **Standard Operating Procedures:** Follow all instructions in `CLAUDE.md`.
- **Project Management:** This project uses `xcodegen`. Always run `xcodegen generate` after adding or removing files.
- **Planning:** For non-trivial tasks, use `enter_plan_mode` to align on the approach before implementation.
- **Context:** Use `docs/plan.md` to track current milestones and `docs/research.md` for architectural context.
